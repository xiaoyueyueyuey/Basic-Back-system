<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xy.admin.mapper.SysConfigMapper">


    <select id="selectConfigOptionSet" resultType="java.lang.String" parameterType="java.lang.Integer">
        SELECT config_options
        FROM sys_config
        WHERE config_id = #{configId}
    </select>
</mapper>